/* import { useAuth0 } from '@auth0/auth0-react';

const LogoutButton = () => {
    const { logout } = useAuth0();

    return (
        <button
            onClick={logout}
            className='text-lg bg-red-400 rounded-md p-1'>
            Logout
        </button>
    )
}

export { LogoutButton }
 */